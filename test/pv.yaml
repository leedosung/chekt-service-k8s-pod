---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: config
  labels:
    vol: chekt-config
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadOnlyMany
  storageClassName: ""
  csi:
    driver: ebs.csi.aws.com
    volumeHandle: vol-06ec1fc42d515aba4
    fsType: xfs
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: app-key
              operator: In
              values:
                - dealer
                #  nodeAffinity:
                #    required:
                #      nodeSelectorTerms:
                #        - matchExpressions:
                #            - key: failure-domain.beta.kubernetes.io/zone
                #              operator: In
                #              values:
                #                - ap-northeast-1c
                #            - key: failure-domain.beta.kubernetes.io/region
                #              operator: In
                #              values:
                #                - ap-northeast-1
